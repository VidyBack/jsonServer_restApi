{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "for creating a backend API server using json-server with ready to deploy config for vercel",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch": "json-server db.json --port=8000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.3"
  }
}
